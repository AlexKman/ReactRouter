import React from "react";
import Modal from "../components/Modal";

const StreamDelete = () => {
  return (
    <div>
      Stream Delete<Modal></Modal>
    </div>
  );
};

export default StreamDelete;
